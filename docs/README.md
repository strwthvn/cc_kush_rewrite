# Документация Modbus регистров

Эта директория содержит автоматически генерируемую документацию карты Modbus регистров.

## 🌐 GitHub Pages

Интерактивная документация доступна по адресу: **https://[ваш-username].github.io/[название-репозитория]/**

## 📋 Возможности веб-интерфейса

- ✅ **Интерактивная таблица** с поиском и сортировкой
- ✅ **Фильтрация** по типу регистра (Holding/Input) и типу данных (BOOL/REAL/INT и т.д.)
- ✅ **Экспорт в Excel** одним кликом
- ✅ **Статистика** по регистрам в реальном времени
- ✅ **Адаптивный дизайн** для просмотра на любых устройствах
- ✅ **Автоматическое обновление** при изменении базы данных

## 🚀 Как это работает

1. **База данных**: `script/db/modbus_registers.db` - SQLite база с картой регистров
2. **Генератор**: `script/generate_html_docs.py` - Python скрипт для генерации HTML
3. **GitHub Actions**: `.github/workflows/deploy-docs.yml` - автоматический деплой
4. **GitHub Pages**: Автоматическая публикация на `https://[username].github.io/[repo]/`

## 🔧 Настройка GitHub Pages

### Шаг 1: Включить GitHub Pages

1. Перейдите в **Settings** вашего репозитория
2. В боковом меню выберите **Pages**
3. В разделе **Source** выберите:
   - Source: **GitHub Actions**
4. Сохраните настройки

### Шаг 2: Запустить деплой

После push изменений в ветку `master` или `main`, GitHub Actions автоматически:
1. Сгенерирует HTML документацию из базы данных
2. Опубликует её на GitHub Pages

Также можно запустить вручную:
1. Перейдите во вкладку **Actions**
2. Выберите workflow **Deploy Modbus Documentation to GitHub Pages**
3. Нажмите **Run workflow**

## 📊 Статистика

Текущая карта регистров:
- **Holding Registers** (SCADA → PLC): 179 регистров
- **Input Registers** (PLC → SCADA): 441 регистр
- **Всего**: 620 регистров
- **Покрытие описаниями**: 100%

## 🛠️ Локальная генерация

Для локальной генерации HTML документации:

```bash
cd script
python3 generate_html_docs.py
```

Результат будет сохранён в `docs/index.html`. Откройте его в браузере для просмотра.

## 📝 Обновление документации

При изменении базы данных `modbus_registers.db`, документация автоматически обновится:

1. **Автоматически**: При push в репозиторий (если изменился файл БД)
2. **Вручную**: Запустите workflow через GitHub Actions

## 🎨 Структура таблицы

| Колонка | Описание |
|---------|----------|
| **Тип** | Holding или Input регистр |
| **Секция** | Группировка регистров (Бункеры, Конвейер, Отвалообразователь и т.д.) |
| **Адрес** | Адрес регистра (для BOOL указан бит: `10.5`) |
| **Тип данных** | BOOL, REAL, INT, UINT, WORD, TIME |
| **Переменная** | Имя переменной в коде PLC |
| **Описание** | Назначение регистра |

## 🔍 Использование фильтров

- **Поиск**: Введите текст в поле поиска для фильтрации по всем колонкам
- **Тип регистра**: Фильтр по Holding или Input регистрам
- **Тип данных**: Фильтр по типу данных (BOOL, REAL и т.д.)
- **Экспорт в Excel**: Скачивает текущий отфильтрованный набор данных
- **Сбросить фильтры**: Очищает все установленные фильтры

## 📦 Технологии

- **Backend**: Python 3, SQLite
- **Frontend**: HTML5, CSS3, JavaScript
- **Библиотеки**: jQuery, DataTables, DataTables Buttons
- **CI/CD**: GitHub Actions
- **Hosting**: GitHub Pages

---

**🤖 Автоматически сгенерировано из базы данных Modbus регистров**

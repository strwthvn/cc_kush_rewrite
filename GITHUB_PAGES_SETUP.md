# 🚀 Настройка GitHub Pages для документации Modbus

## Быстрый старт (3 шага)

### Шаг 1: Включить GitHub Pages
1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** (⚙️ вверху справа)
3. В боковом меню слева выберите **Pages**
4. В разделе **Build and deployment**:
   - **Source**: выберите **GitHub Actions**
5. Готово! Настройка сохранится автоматически

### Шаг 2: Запустить деплой
После включения GitHub Pages, нужно запустить workflow:

**Вариант А: Автоматически (при изменении БД)**
```bash
git add .
git commit -m "Enable GitHub Pages documentation"
git push
```

**Вариант Б: Вручную через GitHub UI**
1. Перейдите во вкладку **Actions**
2. Выберите workflow **Deploy Modbus Documentation to GitHub Pages**
3. Нажмите кнопку **Run workflow** → **Run workflow**

### Шаг 3: Получить URL документации
После успешного деплоя (зелёная галочка ✅), ваша документация будет доступна по адресу:

```
https://[ваш-username].github.io/[название-репозитория]/
```

Например:
- Репозиторий: `https://github.com/dmalikov/newProject`
- Документация: `https://dmalikov.github.io/newProject/`

---

## 📋 Что включено в документацию

✅ **Интерактивная таблица** со всеми 620 Modbus регистрами
✅ **Поиск и фильтрация** по типу регистра и типу данных
✅ **Сортировка** по любой колонке
✅ **Экспорт в Excel** одним кликом
✅ **Статистика** в реальном времени
✅ **Адаптивный дизайн** для мобильных устройств
✅ **Автоматическое обновление** при изменении БД

---

## 🔄 Автоматическое обновление

Документация будет автоматически обновляться при:
- Push изменений в `script/db/modbus_registers.db`
- Push изменений в `script/generate_html_docs.py`
- Ручном запуске workflow через GitHub Actions

---

## 🛠️ Локальный предпросмотр

Для локального просмотра без деплоя:

```bash
cd script
python3 generate_html_docs.py
```

Затем откройте файл `docs/index.html` в браузере.

---

## ❓ Troubleshooting

### Проблема: "GitHub Pages не активирован"
**Решение**: Убедитесь, что в Settings → Pages выбран источник **GitHub Actions**

### Проблема: "Workflow запущен, но сайт не обновился"
**Решение**:
1. Проверьте, что workflow завершился успешно (зелёная галочка)
2. Подождите 1-2 минуты для обновления кеша GitHub Pages
3. Очистите кеш браузера (Ctrl+F5)

### Проблема: "404 Not Found"
**Решение**:
1. Проверьте, что репозиторий публичный (или у вас GitHub Pro/Enterprise)
2. Убедитесь, что workflow выполнился успешно
3. Проверьте правильность URL: `https://[username].github.io/[repo-name]/`

### Проблема: "Permission denied при деплое"
**Решение**: В Settings → Actions → General убедитесь, что:
- ✅ "Allow GitHub Actions to create and approve pull requests" включено
- ✅ "Workflow permissions" установлено в "Read and write permissions"

### Проблема: "Error: deprecated version of actions/upload-artifact"
**Решение**: Эта ошибка уже исправлена в текущей версии workflow файла. Если вы видите эту ошибку:
1. Убедитесь, что `.github/workflows/deploy-docs.yml` использует:
   - `actions/setup-python@v5`
   - `actions/upload-pages-artifact@v3`
   - `actions/deploy-pages@v4`
2. Сделайте `git pull` для получения обновлённой версии

---

## 📊 Технические детали

### Файлы проекта:
```
newProject/
├── .github/workflows/
│   └── deploy-docs.yml          # GitHub Actions workflow
├── script/
│   ├── db/
│   │   └── modbus_registers.db  # SQLite база данных
│   └── generate_html_docs.py    # Генератор HTML
└── docs/
    ├── index.html               # Сгенерированная документация
    └── README.md                # Описание документации
```

### Используемые технологии:
- **Python 3.11**: Генерация HTML из SQLite
- **DataTables**: Интерактивная таблица с поиском/сортировкой
- **GitHub Actions**: Автоматический CI/CD
- **GitHub Pages**: Бесплатный хостинг статических сайтов

---

## ✅ Готово!

После выполнения 3 шагов выше, ваша документация Modbus регистров будет доступна онлайн и будет автоматически обновляться при изменениях в базе данных!

🌐 **URL документации**: `https://[ваш-username].github.io/[название-репозитория]/`
